# branching strategy
The repo should have a default branch that all feature branches are merged back to.
When a new increment starts, the orchestrator should create a new branch with the project-slug
Analize the speckit.specify agent to see how can we make sure that the branch that it creates for the spec is  FROM THE INCREMENT BRANCH and not the repo's default.


